# Sport identity (drives all branding and routing) -- set in Render dashboard, never commit values
SPORT=tennis

# Backend API (the shared brain)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Chatbot (legacy chat widget) -- the same backend URL, served at /chatbot
NEXT_PUBLIC_CHATBOT_URL=http://localhost:3001/chatbot
