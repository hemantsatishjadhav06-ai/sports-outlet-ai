// Site config for PickleballOutlet.in -- this file is intentionally NOT shared.
// Each frontend has its own copy so each deploy is fully self-contained.
import type { SiteConfig } from "@sports-outlet-ai/types";

export const siteConfig: SiteConfig = {
  sport: "pickleball",
  domain: "pickleballoutlet.in",
  brand: "PickleballOutlet.in",
  tagline: "India's Pickleball Headquarters",
  emoji: "🏓",
  primaryColor: "#2563eb",
};
